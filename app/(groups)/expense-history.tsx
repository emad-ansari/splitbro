import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ExpenseHistory() {
  return (
    <SafeAreaView className="">
      <Text>this is expense history screen.</Text>
    </SafeAreaView>
  );
}
